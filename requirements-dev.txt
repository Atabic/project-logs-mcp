# Test and dev dependencies — pinned for CI reproducibility.
# Update with: pip install -U pytest pytest-asyncio respx ruff mypy && pip freeze | grep -E 'pytest|respx|ruff|mypy'
-r requirements.txt
pytest~=9.0
pytest-asyncio~=1.3
respx~=0.22
ruff~=0.15
mypy~=1.19
