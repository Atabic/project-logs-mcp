# ERP MCP Server — Environment Variables
# Copy this file to .env and fill in the values.

# Required: Google OAuth credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Required: Public URL of this MCP server (for OAuth callbacks)
MCP_BASE_URL=https://erp.arbisoft.com

# Optional: ERP API base URL (default: https://erp.arbisoft.com/api/v1/)
# ERP_API_BASE_URL=https://erp.arbisoft.com/api/v1/

# Optional: Port to listen on (default: 8100)
# MCP_PORT=8100

# Optional: Host to bind to (default: 127.0.0.1, Docker sets 0.0.0.0)
# MCP_HOST=127.0.0.1

# Optional: Restrict to this Google Workspace domain (default: arbisoft.com)
# ALLOWED_DOMAIN=arbisoft.com
